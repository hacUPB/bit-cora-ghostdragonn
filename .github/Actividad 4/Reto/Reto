# Actividad 4: Escritura en memoria usando lenguaje ensamblador 

## Objetivo
Aplicar el conocimiento del lenguaje ensamblador para escribir un programa que almacene un valor específico en una dirección de memoria RAM determinada.

## Desarrollo del Reto  
## Enunciado:  
Escribir un programa en lenguaje ensamblador Hack que guarde el número 100 en la posición 32 de la RAM.

## Programa en lenguaje ensamblador:

```
@100 = Cargar el valor 100 en el registro A
D=A = Copiar el valor 100 al registro D
@32 = Cargar la dirección de RAM[32] en el registro A
M=D = Guardar el valor de D (100) en RAM[32]
```
